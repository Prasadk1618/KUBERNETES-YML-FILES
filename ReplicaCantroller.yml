apiVersion: v1
kind: ReplicationCantroller
metadata:
     name: myrc
spec:
   replicas: 3
   selector:
          env: dev
   template:
          metadata:
                labels:
                     env: dev
          spec:
              containers:
                     - name: nginxcontaniers
                       image: nginx
                       Ports:
                           contaninersPort: 80
